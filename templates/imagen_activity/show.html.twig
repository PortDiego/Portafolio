{% extends 'base.html.twig' %}

{% block title %}ImagenActivity{% endblock %}

{% block body %}
    <h1>ImagenActivity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imagen_activity.id }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ imagen_activity.url }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ imagen_activity.date }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_imagen_activity_index') }}">back to list</a>

    <a href="{{ path('app_imagen_activity_edit', {'id': imagen_activity.id}) }}">edit</a>

    {{ include('imagen_activity/_delete_form.html.twig') }}
{% endblock %}
